import 'package:front_mali_event/models/utilisateur.dart';

class UtilisateurProvider {
  // final CollectionReference utilisateursCollection = FirebaseFirestore.instance.collection('utilisateurs');

  Future<void> ajouterUtilisateur(Utilisateur utilisateur) async {
    // return await utilisateursCollection.doc(utilisateur.id).set(utilisateur.toMap());
  }
}
